{
  "name": "django-on-workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pip install -t src/vendor -r vendor.txt && pip install -r requirements-dev.txt",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  }
}
