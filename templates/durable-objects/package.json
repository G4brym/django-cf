{
  "name": "django-on-workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dependencies": "if [ \"$WORKERS_CI\" = \"1\" ]; then sudo apt-get install -y libsqlite3-dev; fi && pip install -t src/vendor -r vendor.txt && pip install -r requirements-dev.txt",
    "deploy": "npm run dependencies && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  }
}
