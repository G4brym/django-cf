{
  "name": "django-on-workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "npm run dependencies && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "file:../../../workers-sdk/packages/wrangler"
  }
}
